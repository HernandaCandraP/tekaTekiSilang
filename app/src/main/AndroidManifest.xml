<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hernanda.tekatekisilang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ttsbg"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Tampilan.Hasil"></activity>
        <activity android:name=".Tampilan.Play" />
        <activity android:name=".Tampilan.Profil" />
        <activity android:name=".Tampilan.Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>